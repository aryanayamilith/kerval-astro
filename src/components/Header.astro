<header class="header">
  <div class="container">
    <!-- Logo -->
    <a href="/" class="logo">
      <img src="/images/logo.svg" alt="Kerval Advisory Partners" />
    </a>

    <!-- Botón hamburguesa -->
    <button class="menu-toggle" aria-label="Abrir menú">☰</button>

    <!-- Navegación -->
    <nav class="nav">
      <ul>
        <li class="dropdown">
          <a href="#">Quiénes Somos</a>
          <ul class="dropdown-menu">
            <li><a href="/">Quiénes Somos</a></li>
            <li><a href="/quienes-somos/equipo">Nuestro Equipo</a></li>
            <li><a href="/quienes-somos/equipo/trabaja">Trabaja con Nosotros</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#">Servicios</a>
          <ul class="dropdown-menu">
            <li><a href="/servicios">Nuestros Servicios</a></li>
            <li><a href="/servicios/clientes">Nuestros Clientes</a></li>
          </ul>
        </li>
        <li><a href="/transacciones">Transacciones</a></li>
        <li><a href="/internacional">Internacional</a></li>
        <li><a href="/blog">Blog y Noticias</a></li>
        <li><a href="/contacto">Contacto</a></li>
      </ul>
    </nav>
  </div>
</header>

<style>
/* ===== HEADER BASE ===== */
.header {
  background: #fff;
  padding: 0.8rem 2rem;
  box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  position: sticky;
  top: 0;
  z-index: 1000;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo img {
  height: 120px; /* tamaño equilibrado del logo */
}

/* ===== NAV BASE ===== */
.nav ul {
  display: flex;
  gap: 1.5rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

.nav a {
  text-decoration: none;
  color: #c00000;
  font-family: 'Calibri', sans-serif;
  font-size: 17px;   
  font-weight: 600;
  transition: color 0.2s ease;
}

.nav a:hover {
  color: #002060;
}

/* ===== DROPDOWN ===== */
.dropdown {
  position: relative;
}

.dropdown-menu {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  background: #fff; /* mismo fondo que header */
  padding: 0.5rem 0;
  list-style: none;
  min-width: 220px;
  z-index: 999;
  border: 1px solid #ddd;
  border-radius: 4px;

  opacity: 0;
  transform: translateY(10px);
  transition: all 0.2s ease-in-out;
}

.dropdown-menu li { margin: 0; }

.dropdown-menu a {
  display: block;
  padding: 0.6rem 1rem;
  color: #c00000;
  font-family: 'Calibri', sans-serif;
  font-size: 16px;
  white-space: nowrap;
}

.dropdown-menu a:hover {
  background: #f5f5f5;
  color: #002060;
}

/* Hover en desktop */
@media (min-width: 769px) {
  .dropdown:hover .dropdown-menu {
    display: block;
    opacity: 1;
    transform: translateY(0);
  }
}

/* ===== MOBILE ===== */
.menu-toggle {
  display: none;
  background: none;
  border: none;
  font-size: 1.8rem;
  cursor: pointer;
}

@media (max-width: 768px) {
  .menu-toggle { display: block; }
  .nav {
    display: none;
    flex-direction: column;
    width: 100%;
    background: #fff;
    position: absolute;
    top: 100%;
    left: 0;
    padding: 1rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.15);
  }
  .nav.active { display: flex; }
  .nav ul { flex-direction: column; gap: 1rem; }
  .dropdown-menu {
    position: static;
    border: none;
    background: #f9f9f9;
    opacity: 1;
    transform: translateY(0);
  }
}
</style>
